# Backend base URL (Render). Leave unset locally to use http://localhost:8000
VITE_API_URL=https://your-backend.onrender.com

# Sentry error tracking (optional). Unset = no-op; set in Vercel env vars for production.
VITE_SENTRY_DSN=
