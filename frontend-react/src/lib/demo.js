/* The "try an example" first-touch flow: a sample document bundled in
   public/ (served by the frontend, never touching backend/data/uploads)
   plus the question that shows off retrieval + citations best. */
export const DEMO_DOC_PATH = 'sample-postmortem.md'
export const DEMO_DOC_NAME = 'incident-postmortem-2025-03.md'
export const DEMO_QUESTION = 'How long did the outage last, and what was the root cause?'
